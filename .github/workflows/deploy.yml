name: Build and deploy ASP.NET Core app to Azure Web App - ProyectoFinalApi

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Build
        run: dotnet build --configuration Release

      - name: Publish
        run: dotnet publish -c Release -o publish_output

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: publish_output

  deploy:
    runs-on: windows-latest
    needs: build

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: e9c389da-d96f-43fd-8add-8c58a33caa15    # TU MANAGED ID
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}        # CREA ESTE
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}  # CREA ESTE

      - name: Deploy to App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'ProyectoFinalApi'
          package: .


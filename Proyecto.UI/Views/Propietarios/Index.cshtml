@model IEnumerable<Proyecto.Model.Vehiculo>

@{
    ViewData["Title"] = "Vehículos y Propietarios";
}

<h1>Vehículos y Propietarios</h1>
<p class="text-muted">Gestión de dueños de vehículos.</p>

<table class="table table-hover table-bordered table-datatable">
    <thead class="table-light">
        <tr>
            <th>Placa</th>
            <th>Marca</th>
            <th>Modelo</th>
            <th>Dueño Actual</th>
            <th>Identificación</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Placa)</td>
                <td>@Html.DisplayFor(modelItem => item.Marca)</td>
                <td>@Html.DisplayFor(modelItem => item.Modelo)</td>
                <td>@(item.Persona?.Nombre ?? "N/A")</td>
                <td>@(item.Persona?.Identificacion ?? "N/A")</td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm btn-confirmar-editar">
                            <i class="fa-solid fa-user-pen"></i> Editar Dueño
                        </a>

                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="form-eliminar">
                            <button type="submit" class="btn btn-danger btn-sm">
                                <i class="fa-solid fa-trash"></i> Eliminar
                            </button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        $(document).ready(function () {
            var successMessage = '@Html.Raw(TempData["SuccessMessage"])';
            if (successMessage) mostrarAlertaExito(successMessage);

            var errorMessage = '@Html.Raw(TempData["ErrorMessage"])';
            if (errorMessage) mostrarAlertaError(errorMessage);

            $('.btn-confirmar-editar').click(function (e) {
                e.preventDefault();
                var urlDestino = $(this).attr('href');

                Swal.fire({
                    title: '¿Cambiar de Propietario?',
                    text: "Irás a la pantalla para asignar un nuevo dueño.",
                    icon: 'question',
                    showCancelButton: true,
                    confirmButtonColor: '#f0ad4e',
                    cancelButtonColor: '#3085d6',
                    confirmButtonText: 'Sí, continuar',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        window.location.href = urlDestino;
                    }
                });
            });

            $('.form-eliminar').submit(function (e) {
                e.preventDefault();
                var form = this;
                Swal.fire({
                    title: '¿Eliminar Vehículo?',
                    text: "Se eliminará el registro de este vehículo.",
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#d33',
                    cancelButtonColor: '#3085d6',
                    confirmButtonText: 'Sí, eliminar',
                    cancelButtonText: 'Cancelar'
                }).then((result) => {
                    if (result.isConfirmed) {
                        form.submit();
                    }
                });
            });
        });
    </script>
}